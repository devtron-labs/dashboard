{
    "name": "devtron-frontend",
    "version": "1.0.0",
    "scripts": {
        "build": "pnpm run -r build",
        "lint": "pnpm run -r lint"
    },
	"devDependencies": {
		"husky": "^7.0.4"
	}
}
